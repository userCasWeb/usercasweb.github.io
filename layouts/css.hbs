{{>header }}
  <h1 class="padding-top-4 front">{{ title }}</h1>
  <p class="h5 excerpt">{{ excerpt }}</p>
  <main class="">
    {{{ contents }}}
    {{#if collections.css }}
      <ul class="grid-2">
      {{#each collections.css }}
        <li>
          <h5><a href="/{{path}}">{{ title }}</a></h5>
          <span class="dateTag">{{ formattedDate date }} @ {{ collections }}</span>
          <p>{{ excerpt }}</p>
        </li>
      {{/each}}
      </ul>
    {{/if}}
  </main>
{{>footer }}